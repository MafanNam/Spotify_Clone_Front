import Spinner from "@/components/general/Spinner";

export default function FullScreenSpinner() {
  return (
    <div className="w-full min-h-[60dvh] flex items-center justify-center px-4">
      <Spinner/>
    </div>
  )
}